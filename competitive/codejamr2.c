#include<stdio.h>
#include<math.h>
#define slld(x) scanf("%lld",&x)
#define sd(x) scanf("%d",&x)
#define sld(x) scanf("%ld",&x)
#define ss(x)  scanf("%s",x)
long long rev(long long x ){
    long long c=0,t,p=0 ;
    while(x>0)
    {
        t=x%10 ;
        c=c+t*pow(10,p++) ;


    }
}
int main(){
long long t,c ,n,x,y,z,i,j,k ,l,m,p ;

slld(t) ;
while(t--){
    slld(n) ;
    c=0 ;
    x=1 ;
    while(1){
        c++ ;
        if()

    }
}
return 0 ;
}

